version: '3.8'
services:
  # Development environment setup
  postgres:
    image: postgres:14
    environment:
      POSTGRES_DB: careerlaunch
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
